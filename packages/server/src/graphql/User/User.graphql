type User implements Resource & Timestampable {
  id: ID!
  name: String!
  info: String
  email: String!
  avatar: Avatar!
  ownedBookCopies: [BookCopy!]!
  borrowedBookCopies: [BookCopy!]!
  createdAt: String!
  updatedAt: String!
}

type Avatar {
  image: Image!
  color: String!
}
