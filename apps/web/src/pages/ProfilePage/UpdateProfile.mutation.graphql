mutation UpdateProfile($input: UpdateProfileInput!) {
  updateProfile(input: $input) {
    __typename

    ... on ClassifiedUser {
      ...CurrentUser
    }

    ... on ValidationErrors {
      errors {
        path
        message
      }
    }
  }
}
