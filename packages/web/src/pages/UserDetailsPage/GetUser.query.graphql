query GetUser($id: ExternalID!) {
  user(id: $id) {
    ... on UserNotFoundError {
      __typename
      message
    }

    ... on User {
      id
      name
      info
      avatar {
        ...Avatar
      }
      ownedBookCopies {
        ...BookCopy
      }
      borrowedBookCopies {
        ...BookCopy
      }
    }
  }
}
