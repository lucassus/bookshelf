query GetAuthor($id: ID!) {
  author(id: $id) {
    name
    bio
    photo {
      url
    }

    books {
      ...bookFields
    }
  }
}

# TODO: Figure out how to reuse it
fragment bookFields on Book {
  id
  title
  favourite
  cover {
    url
  }
  author {
    id
    name
  }
}
