query GetAuthor($id: ExternalID!) {
  author(id: $id) {
    ... on AuthorNotFoundError {
      __typename
      message
    }

    ... on Author {
      name
      bio
      photo {
        url
      }

      books {
        ...BookCard
      }
    }
  }
}
