type User implements Resource {
  id: ID!
  name: String!
  info: String
  email: String!
  avatar: Avatar!
  ownedBookCopies: [BookCopy!]!
  borrowedBookCopies: [BookCopy!]!
}

type Avatar implements WithImage {
  image: Image!
  color: String!
}
