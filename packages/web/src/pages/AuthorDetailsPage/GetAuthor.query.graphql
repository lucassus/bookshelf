query GetAuthor($id: ExternalID!) {
  author(id: $id) {
    ... on ResourceNotFoundError {
      __typename
      message
    }

    ... on Author {
      name
      bio
      photo {
        url
      }

      books {
        ...BookCard
      }
    }
  }
}
