query GetUser($id: ID!) {
  user(id: $id) {
    id
    name
    email
    info
    avatar {
      ...Avatar
    }
    ownedBookCopies {
      ...BookCopy
    }
    borrowedBookCopies {
      ...BookCopy
    }
  }
}
