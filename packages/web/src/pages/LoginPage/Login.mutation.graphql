mutation Login($input: LoginInput!) {
  login(input: $input) {
    ... on LoginSuccess {
      __typename
      currentUser {
        ...CurrentUser
      }
    }

    ... on LoginFailure {
      __typename
      message
    }
  }
}
