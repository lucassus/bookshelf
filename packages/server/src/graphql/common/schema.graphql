scalar ExternalID

interface Resource {
  id: ExternalID!
}

interface Timestampable {
  createdAt: String!
  updatedAt: String!
}

type Image {
  path: String!
  url: String!
}

extend type Query {
  resource(id: ID!): Resource!
}
