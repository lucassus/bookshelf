{
  "name": "@bookshelf/fake-server",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@bookshelf/secure-id": "1.0.0",
    "@bookshelf/string-utils": "1.0.0",
    "@graphql-tools/mock": "7.0.0",
    "@graphql-tools/schema": "7.0.0",
    "@types/faker": "6.6.9",
    "apollo-server": "3.12.0",
    "faker": "6.6.6",
    "graphql": "16.6.0",
    "ts-node-dev": "2.0.0",
    "typescript": "5.0.4"
  },
  "scripts": {
    "clean": "yarn rimraf coverage dist *.tsbuildinfo",
    "typecheck": "yarn tsc --noEmit",
    "build": "yarn tsc --build",
    "dev": "yarn ts-node-dev --no-notify --respawn --transpile-only ./src/index.ts",
    "lint": "yarn eslint . --ext .ts"
  }
}
