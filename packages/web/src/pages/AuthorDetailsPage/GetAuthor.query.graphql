query GetAuthor($id: ExternalID!) {
  author(id: $id) {
    ... on AuthorNotFoundError {
      message
    }

    ... on Author {
      name
      bio
      photo {
        url
      }

      books {
        ...BookCard
      }
    }
  }
}
