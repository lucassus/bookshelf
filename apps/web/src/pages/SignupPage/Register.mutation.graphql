mutation Register($input: RegistrationInput!) {
  register(input: $input) {
    __typename

    ... on ClassifiedUser {
      ...CurrentUser
    }

    ... on ValidationErrors {
      errors {
        path
        message
      }
    }
  }
}
