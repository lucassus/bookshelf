mutation Register($input: RegistrationInput!) {
  register(input: $input) {
    __typename

    ... on FullUserInfo {
      ...CurrentUser
    }

    ... on ValidationErrors {
      errors {
        path
        message
      }
    }
  }
}
