mutation UpdateProfile($input: UpdateProfileInput!) {
  updateProfile(input: $input) {
    __typename

    ... on CurrentUser {
      ...CurrentUser
    }

    ... on UpdateProfileFailure {
      validationErrors {
        path
        message
      }
    }
  }
}
