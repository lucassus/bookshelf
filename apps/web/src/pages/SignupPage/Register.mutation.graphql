mutation Register($input: RegistrationInput!) {
  register(input: $input) {
    __typename

    ... on User {
      ...CurrentUser
    }

    ... on ValidationErrors {
      errors {
        path
        message
      }
    }
  }
}
