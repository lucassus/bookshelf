{
  "name": "@bookshelf/secure-id",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "private": true,
  "devDependencies": {
    "@types/jest": "26.0.13",
    "jest": "26.4.2",
    "ts-jest": "26.3.0",
    "typescript": "4.0.2"
  },
  "scripts": {
    "clean": "rimraf dist && rimraf covarage",
    "compile": "tsc -p tsconfig.build.json",
    "build": "yarn clean && yarn compile",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "lint": "eslint . --ext .ts",
    "test": "jest"
  }
}
