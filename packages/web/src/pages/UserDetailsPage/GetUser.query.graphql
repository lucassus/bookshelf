query GetUser($id: ExternalID!) {
  user(id: $id) {
    id
    name
    email
    info
    avatar {
      ...Avatar
    }
    ownedBookCopies {
      ...BookCopy
    }
    borrowedBookCopies {
      ...BookCopy
    }
  }
}
